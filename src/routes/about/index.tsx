import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div>
      <h1>About</h1>
      <article>
        Just a regular Todo app using "Qwik".
        <br />
        They really need to update their documentation because its a complete mess
      </article>
    </div>
  );
});
